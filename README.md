The Railway Reservation System is a terminal-based Python application designed to simulate 
real-time passenger ticketing workflows and berth allocation management. 
Built specifically around local text-file database simulation, 
this engine manages data persistence, 
auto-generates sequential seat assignments within a 100-seat constraint limit, 
and maintains a strict transaction history ledger.
